# 画面設計書：ENKATSU 〜園活を円滑に〜

## 1. 目的

本ドキュメントは、ENKATSU の画面構成・各画面の役割・表示項目・操作内容を整理し、チーム全員が同じ認識で実装を進められるようにするための画面設計書である。

ENKATSU は、共働き家庭・仕事復帰を控えた保護者向けに、保育園・こども園の「復職後の保護者負担」を生活負担・時間負担という2軸で検索・比較できる Web アプリケーションである。

なお、園詳細画面では園側の情報がネガティブなイメージにならないよう、「毎日の準備」「仕事との両立」という表現を使用する。比較画面では保護者目線で負担度合いを比較する目的から、「生活負担」「時間負担」という表現を使用する。

未登録ユーザーは園一覧・園情報を閲覧でき、一般ユーザーはお気に入り登録・2園比較、プレミアムユーザーはお気に入り無制限登録・3園比較・希望条件との一致表示・サポート情報閲覧ができる。

---

## 2. 画面一覧

### 2.1 未登録ユーザー画面

| URL               | 画面名      | 概要                                           |
| ----------------- | -------- | -------------------------------------------- |
| `/`               | ホーム画面    | サービス概要・保育園名／住所検索・条件で検索・園一覧を表示する              |
| `/schools/search` | 検索結果画面   | 保育園名・住所・条件での検索結果を一覧表示する                      |
| `/schools/[id]`   | 園詳細画面    | 個別の園の基本情報・毎日の準備・仕事との両立を閲覧する（サポート情報はオーバーレイ表示） |
| `/login`          | ログイン画面   | 登録済みユーザーがログインする                              |
| `/register`       | 会員登録画面   | 新しいユーザーがアカウントを作成する                           |
| `/plans`          | プラン・料金画面 | 会員でない方・会員・プレミアムの機能を比較表示する                    |
| `404`             | 404エラー画面 | 存在しないページへのアクセス時に表示する                         |
| `loading`         | ローディング画面 | データ取得中に表示する                                  |

### 2.2 一般ユーザー画面

| URL                        | 画面名            | 概要                                                |
| -------------------------- | -------------- | ------------------------------------------------- |
| `/`                        | ホーム画面（ログイン済み）  | おすすめ表示・保育園名／住所検索・条件で検索・園一覧を表示する                   |
| `/schools/search`          | 検索結果画面         | 保育園名・住所・条件での検索結果を一覧表示する                           |
| `/schools/[id]`            | 園詳細画面          | 個別の園情報を閲覧する（サポート情報はオーバーレイ表示）                      |
| `/mypage/favorites`        | お気に入り一覧画面      | お気に入り登録した園の一覧・比較対象の選択を行う（最大5件登録・2園比較）             |
| `/compare`                 | 比較画面           | 選択した2園を横並び比較する（一致表示なし）                            |
| `/mypage`                  | マイページ          | ユーザー情報・現在のプランでできること・プレミアム誘導を表示する                  |
| `/mypage/edit`             | プロフィール編集画面     | ユーザー情報・希望条件を編集する                                  |
| `/payment/complete`        | プレミアム登録完了画面    | 決済完了メッセージとホームへの導線を表示する                            |
| `/payment/cancel-complete` | プレミアムキャンセル完了画面 | Stripe Customer Portal からの戻り先として、一般プランへの変更完了を表示する |

### 2.3 プレミアムユーザー画面

| URL                        | 画面名            | 概要                                    |
| -------------------------- | -------------- | ------------------------------------- |
| `/`                        | ホーム画面（プレミアム）   | おすすめ表示・保育園名／住所検索・条件で検索・園一覧を表示する       |
| `/schools/search`          | 検索結果画面         | 保育園名・住所・条件での検索結果を一覧表示する               |
| `/schools/[id]`            | 園詳細画面          | サポート情報を含む全情報を閲覧できる                    |
| `/mypage/favorites`        | お気に入り一覧画面      | お気に入り登録した園の一覧・比較対象の選択を行う（無制限登録・3園比較）  |
| `/compare`                 | 比較画面（3園・一致表示）  | 選択した3園を横並び比較・希望条件と一致する項目をハイライト表示する    |
| `/mypage`                  | マイページ（プレミアム）   | プレミアム状態・現在のプランでできること・一般プランへの変更導線を表示する |
| `/mypage/edit`             | プロフィール編集画面     | ユーザー情報・希望条件を編集する                      |
| `/payment/complete`        | プレミアム登録完了画面    | 決済完了メッセージとホームへの導線を表示する                |
| `/payment/cancel-complete` | プレミアムキャンセル完了画面 | 一般プランへの変更完了を表示する                      |

### 2.4 MVP では作成しない画面

| 画面・機能                        | 内容              |
| ---------------------------- | --------------- |
| 詳細比較レポート画面（`/report`）        | 比較画面までで対応のため対象外 |
| 口コミ投稿・承認画面                   | MVP 対象外         |
| 人気ランキング画面                    | MVP 対象外         |
| AI レポート生成画面                  | MVP 対象外         |
| 地図機能                         | MVP 対象外         |
| PDF 出力機能                     | MVP 対象外         |
| 請求履歴表示・プラン変更（アップグレード以外）      | MVP 対象外         |
| 管理画面 CRUD                    | MVP 対象外         |
| `/compare/select`（比較リスト選択画面） | お気に入り一覧に統合済み    |

---

## 3. 共通設計

### 3.1 共通レイアウト

**全画面共通**

```txt
Header（ロゴ・ハンバーガーメニュー）
↓
Main Content
```

フッターは設置しない。

**ハンバーガーメニューの中身**

未登録ユーザー：

```txt
アカウント
[ 新規会員登録 ] [ ログイン ]

マイ機能
ホーム
お気に入り/比較（ログイン後に利用可）
マイページ（ログイン後に利用可）
プラン・料金確認
```

一般ユーザー：

```txt
ホーム            → /
お気に入り/比較   → /mypage/favorites
マイページ        → /mypage
プラン・料金確認  → /plans
─────────────
ログアウト
```

プレミアムユーザー：

```txt
ホーム            → /
お気に入り/比較   → /mypage/favorites
マイページ        → /mypage
プラン・料金確認  → /plans
─────────────
ログアウト
```

**ハンバーガーメニューの実装方針**

コンポーネントは1つ。ログイン状態・ユーザー区分で中身を出し分ける。

```tsx
{
  !user && <Link href="/register">新規会員登録</Link>;
}
{
  !user && <Link href="/login">ログイン</Link>;
}
{
  user && <Link href="/mypage/favorites">お気に入り/比較</Link>;
}
{
  user && <Link href="/mypage">マイページ</Link>;
}
<Link href="/plans">プラン・料金確認</Link>;
{
  user && <button onClick={logout}>ログアウト</button>;
}
```

### 3.2 共通 UI コンポーネント

| コンポーネント         | 用途                                  |
| --------------- | ----------------------------------- |
| Button          | CTA・登録・削除・遷移などの操作ボタン                |
| SchoolCard      | 園一覧・お気に入り一覧での園情報表示。カード全体がリンクとして機能する |
| FavoriteButton  | お気に入り登録・解除ボタン（ハートマーク・全ユーザー利用可）      |
| CompareCheckbox | お気に入り一覧での比較対象選択チェックボックス             |
| SearchBar       | 保育園名・住所検索の入力欄                       |
| FilterPanel     | 開閉式の「条件で検索」フォーム（毎日給食・おむつ廃棄・布団持参なし等） |
| Modal           | 確認ダイアログ・プレミアム誘導など                   |
| Toast           | 操作フィードバック（成功・エラー・警告）                |
| Skeleton        | データ取得中のプレースホルダー                     |
| EmptyState      | データが存在しない場合の表示                      |
| ErrorMessage    | 入力エラー・API エラーの表示                    |
| Loading         | ページ・API 取得中のスピナー                    |
| LockOverlay     | プレミアム限定機能の制限表示（テキスト付きオーバーレイ）        |
| MatchBadge      | 希望条件との一致表示（プレミアムのみ）                 |

### 3.3 エラー表示方針

* 入力値に誤りがある場合は、フォーム項目の直下にエラーメッセージを表示する
* API 通信に失敗した場合は、画面上部または操作付近に Toast / エラーバナーを表示する
* 未登録ユーザーが認証必須画面にアクセスした場合は `/login` へ遷移する
* 一般ユーザーがプレミアム限定機能にアクセスした場合は LockOverlay を表示し `/plans` へ誘導する
* 存在しない URL へアクセスした場合は 404 ページへ遷移する
* 検索結果が0件の場合は「条件に合う園が見つかりませんでした」を表示する

### 3.4 ローディング表示方針

* API からデータ取得中は Skeleton コンポーネントを表示する
* ボタン操作中はボタン内スピナー + disabled 状態にして二重送信を防ぐ
* ページ遷移中はローディング画面を表示する
* データが存在しない場合は EmptyState でメッセージを表示する

---

## 4. 画面詳細

### 4.1 ホーム画面（未登録ユーザー）

**URL**

`/`

**目的**

未登録ユーザーが ENKATSU のサービス概要を把握し、保育園名・住所または条件で検索、または園一覧から園を探せるようにする。

**表示項目**

| 項目         | 内容                                                                |
| ---------- | ----------------------------------------------------------------- |
| キャッチコピー    | サービスの価値を一言で表す                                                     |
| サービス説明     | 生活負担・時間負担という2軸での園比較サービスであることを説明する                                 |
| メイン検索フォーム  | 保育園名・住所で検索できる                                                     |
| 条件で検索（開閉式） | 毎日給食／おむつ廃棄／布団持参なし／延長保育の利用時間〜19時まで／平日行事なし／保護者会なし／園内習い事あり／アレルギー対応あり |
| 園一覧        | SchoolCard（タグ付き）を一覧表示する                                           |

**操作**

* 保育園名・住所を入力して検索する → `/schools/search` へ遷移
* 「条件で検索」を開いて条件を選択し検索する → `/schools/search` へ遷移（条件をクエリに付与）
* SchoolCard（カード全体）をクリックすると `/schools/[id]` へ遷移する
* FavoriteButton（ハート）をクリックするとログイン誘導モーダルを表示する

---

### 4.2 ホーム画面（一般・プレミアムユーザー）

**URL**

`/`

**目的**

ログイン済みユーザーが保育園名・住所または条件で検索、またはおすすめの園を探せるようにする。

**表示項目**

| 項目         | 内容                                                                |
| ---------- | ----------------------------------------------------------------- |
| キャッチコピー    | サービスの価値を一言で表す                                                     |
| メイン検索フォーム  | 保育園名・住所で検索できる                                                     |
| 条件で検索（開閉式） | 毎日給食／おむつ廃棄／布団持参なし／延長保育の利用時間〜19時まで／平日行事なし／保護者会なし／園内習い事あり／アレルギー対応あり |
| おすすめの園一覧   | おすすめロジックに基づいた SchoolCard（タグ付き）を表示する                               |

**おすすめ表示ロジック**

```txt
ステップ1：エリアで絞り込み
ユーザーの住所（市区町村）と同じエリアの園を抽出する

ステップ2：希望条件で並び替え
・エリア該当あり、かつ希望条件設定済み → 一致度が高い順に表示
・エリア該当あり、かつ希望条件未設定 → そのまま表示
・エリア該当が0件 → エリア絞り込みを行わず、希望条件のみで一致度順に表示
  （希望条件も未設定の場合は全件表示）
```

**Redisキャッシュ**

ホーム画面のおすすめ表示・園一覧取得では、Redisキャッシュを利用する。

* 園一覧の元データをキャッシュする
* 検索条件・並び順ごとにキャッシュキーを分ける
* ログインユーザーごとに変わるお気に入り状態や会員状態は、キャッシュ対象と分けて扱う

**操作**

* 保育園名・住所を入力して検索する → `/schools/search` へ遷移
* 「条件で検索」を開いて条件を選択し検索する → `/schools/search` へ遷移（条件をクエリに付与）
* SchoolCard（カード全体）をクリックすると `/schools/[id]` へ遷移する
* FavoriteButton（ハート）でお気に入り登録・解除する

---

### 4.3 検索結果画面

**URL**

`/schools/search`

**目的**

全ユーザーが保育園名・住所・条件で絞り込んだ結果を一覧表示し、詳細画面へ進めるようにする。画面上部にはメイン検索バーと条件で検索を表示し、再検索もこの画面内で行える。

**利用 API**

| メソッド | エンドポイント    | 内容                    |
| ---- | ---------- | --------------------- |
| GET  | `/schools` | 検索結果取得（Redis キャッシュ必須） |

**検索条件**

| 区分         | 条件                                                                |
| ---------- | ----------------------------------------------------------------- |
| メイン検索      | 保育園名・住所                                                           |
| 条件で検索（開閉式） | 毎日給食／おむつ廃棄／布団持参なし／延長保育の利用時間〜19時まで／平日行事なし／保護者会なし／園内習い事あり／アレルギー対応あり |

**表示項目（SchoolCard）**

| 項目             | 内容                                          |
| -------------- | ------------------------------------------- |
| 写真             | 園の画像                                        |
| 園名             | 園の名称                                        |
| 住所             | 所在地                                         |
| 電話番号           | 園の電話番号                                      |
| タグ             | 検索条件に該当するもののみタグ表示（例：「保育園」「毎日給食」「園内習い事あり」など） |
| FavoriteButton | お気に入り登録・解除（ハートマーク・全ユーザー利用可）                 |

SchoolCard はカード全体がリンクとして機能し、クリックすると `/schools/[id]` へ遷移する（詳細ボタンは設置しない）。

**Redisキャッシュ**

検索結果画面では、園一覧・検索結果の取得にRedisキャッシュを利用する。

* `keyword`
* `area`
* 条件検索項目
* `sort`
* `limit`
* `offset`

などをもとにキャッシュキーを生成する。

ただし、`isFavorited` などログインユーザーごとに変わる情報は、キャッシュされた園データとは分けて付与する。

**お気に入り件数制限**

| ユーザー区分    | 上限               |
| --------- | ---------------- |
| 未登録ユーザー   | 利用不可（ログイン誘導モーダル） |
| 一般ユーザー    | 5件まで             |
| プレミアムユーザー | 無制限              |

上限に達した状態でハートをクリックした場合、Toast で「お気に入りは5件まで登録できます。プレミアムで無制限に」と表示し `/plans` へ誘導する。

**空状態**

検索結果が0件の場合、以下を表示する。

```txt
条件に合う園が見つかりませんでした
検索条件を変更してもう一度お試しください
```

**画面イメージ**

```txt
[ENKATSU]                               ☰

[ 園の名前か住所で検索 ]        [検索]

▼条件で検索
☐ 毎日給食              ☐ 園内習い事あり
☐ おむつ廃棄            ☐ アレルギー対応あり
☐ 布団持参なし
☐ 延長保育の利用時間〜19時まで
☐ 平日行事なし
☐ 保護者会なし
                                [ 検索 ]

─────────────────────────────
[写真] さくら保育園
       東京都千代田区...
       電話番号：...
       [保育園] [毎日給食]              ♡

[写真] さくら保育園
       ...
─────────────────────────────
```

---

### 4.4 園詳細画面

**URL**

`/schools/[id]`

**目的**

全ユーザーが個別の園の基本情報・毎日の準備・仕事との両立を閲覧できるようにする。サポート情報はプレミアム限定とする。園詳細画面では園側の情報がネガティブなイメージにならないよう「毎日の準備」「仕事との両立」という表現を使用する（比較画面では「生活負担」「時間負担」を使用）。

**利用 API**

| メソッド | エンドポイント        | 内容    |
| ---- | -------------- | ----- |
| GET  | `/schools/:id` | 園詳細取得 |

**表示項目**

| カテゴリ   | 項目                              | 未登録・一般ユーザー   | プレミアムユーザー |
| ------ | ------------------------------- | ------------ | --------- |
| 基本情報   | 園名・住所・電話番号・園種別                  | ✅            | ✅         |
| タグ     | 検索条件に該当する特徴タグ                   | ✅            | ✅         |
| 毎日の準備  | 給食・弁当 / 持ち物 / おむつ対応 / 布団対応      | ✅            | ✅         |
| 仕事との両立 | 延長保育の時間 / 延長保育利用者 / 平日行事 / 保護者会 | ✅            | ✅         |
| サポート情報 | 連絡帳 / 欠席連絡方法 / 園内習い事 / アレルギー対応  | 項目名のみ＋オーバーレイ | ✅ 内容閲覧可   |
| 園の特徴   | 園の特徴・補足説明                       | ✅            | ✅         |

**仕事との両立の表示形式**

```txt
─ 仕事との両立 ─────────────────
延長保育の時間：  18:00〜20:00
延長保育利用者：  3人程度
平日行事：        なし
保護者会：        月に1回
```

**サポート情報の制限表示**

未登録ユーザー・一般ユーザーには項目名のみを表示し、値の部分にスモーク（ぼかし）をかけた上に「プレミアムユーザーに登録すれば閲覧可能」というテキスト付きのオーバーレイを重ねて表示する。

誘導先はユーザー区分によって分ける。

| ユーザー区分    | タップ時の挙動                          |
| --------- | -------------------------------- |
| 未登録ユーザー   | 会員登録誘導モーダルを表示し、`/register` へ誘導する |
| 一般ユーザー    | プレミアム誘導モーダルを表示し、`/plans` へ誘導する   |
| プレミアムユーザー | サポート情報の内容を表示するため、オーバーレイは表示しない    |

```txt
─ サポート情報 ─────────────────
連絡帳
欠席連絡方法         ┌──────────────────┐
園内習い事           │ 🔒               │
アレルギー対応       │ プレミアムユーザーに│
                     │ 登録すれば閲覧可能 │
                     └──────────────────┘
```

**機能**

* FavoriteButton（ハート）でお気に入り登録・解除できる（全ユーザー利用可、件数制限あり）
* 比較は行わない（比較対象の選択は `/mypage/favorites` で行う）

**操作**

* 各情報を閲覧する
* FavoriteButton（ハート）をクリックしてお気に入り登録・解除する
* 未登録ユーザーがハートをクリックした場合はログイン誘導モーダルを表示する
* 未登録ユーザーがサポート情報ロックをタップした場合は、会員登録誘導モーダルを表示する
* 一般ユーザーがサポート情報ロックをタップした場合は、プレミアム誘導モーダルを表示する
* 存在しない ID へアクセスした場合は 404 ページへ遷移する

**画面イメージ**

```txt
[ENKATSU]                               ☰

< 戻る

[写真]

さくら保育園                        ♡
住所：東京都千代田区xxxxxx
電話番号：xxxxxxxxxx
[保育園] [毎日給食] [園内習い事あり]

─ 毎日の準備 ───────────────────
給食・弁当：    毎日給食あり
持ち物：        タオル・着替え・おむつ
おむつ対応：    園で廃棄
布団対応：      掛布団、敷布団持参

─ 仕事との両立 ─────────────────
延長保育の時間： 18:00〜20:00
延長保育利用者： 3人程度
平日行事：       なし
保護者会：       月に1回

─ サポート情報 ─────────────────
連絡帳
欠席連絡方法         ┌──────────────────┐
園内習い事           │ 🔒               │
アレルギー対応       │ プレミアムユーザーに│
                     │ 登録すれば閲覧可能 │
                     └──────────────────┘

─ 園の特徴 ─────────────────────
園の特徴・補足説明テキスト...
```

---

### 4.5 お気に入り一覧画面

**URL**

`/mypage/favorites`

**目的**

ログイン済みユーザーがお気に入り登録した園を一覧で確認し、比較対象を選択できるようにする。ハンバーガーメニュー「お気に入り/比較」から遷移する。

**利用 API**

| メソッド   | エンドポイント                         | 内容         |
| ------ | ------------------------------- | ---------- |
| GET    | `/users/me/favorites`           | お気に入り園一覧取得 |
| DELETE | `/users/me/favorites/:schoolId` | お気に入り解除    |

**対象ユーザー**

一般・プレミアムユーザー。未登録ユーザーがアクセスした場合は `/login` へ遷移する。

**表示項目**

| 項目              | 内容                               |
| --------------- | -------------------------------- |
| 園カード            | 写真・園名・住所・種別・タグを表示する              |
| CompareCheckbox | 比較対象として選択する                      |
| 解除ボタン           | お気に入りを解除する                       |
| 比較するボタン         | `/compare` へ遷移する（選択数が条件を満たすと有効化） |
| 登録件数表示          | 「3 / 5件登録中」など                    |

**比較対象選択数**

| ユーザー区分    | 選択数 | 登録上限 |
| --------- | --- | ---- |
| 一般ユーザー    | 2園  | 5件まで |
| プレミアムユーザー | 3園  | 無制限  |

**空状態**

```txt
お気に入りに登録した園はまだありません。
[ 園を探す ]
```

**画面イメージ（一般ユーザー）**

```txt
[ENKATSU]                               ☰

お気に入りの園        3 / 5件登録中

☑ [写真] ○○保育園
         △△区 / 認可
         [保育園] [毎日給食]
         [ 解除 ]

☐ [写真] △△こども園
         □□区 / 認定
         [ 解除 ]

[ 比較する（2園選択で有効） ]
```

---

### 4.6 比較画面

**URL**

`/compare`

**目的**

ログイン済みユーザーが選択した園を横並びで比較できるようにする。プレミアムユーザーのみ希望条件との一致状況をハイライト表示する。比較画面では保護者目線で負担度合いを比較する目的から「生活負担」「時間負担」という表現を使用する。

**利用 API**

| メソッド | エンドポイント                      | 内容                   |
| ---- | ---------------------------- | -------------------- |
| GET  | `/schools/compare?ids=1,2`   | 比較対象園の情報取得           |
| GET  | `/schools/compare?ids=1,2,3` | プレミアムユーザー向けの3園比較情報取得 |

**比較対象の受け渡し**

比較対象は `/mypage/favorites` で選択し、選択した園IDを `/compare` へ渡す。

MVPでは比較対象をDB保存しない。
比較画面では、受け取った園IDをもとに `GET /schools/compare?ids=...` で比較情報を取得する。

**比較項目**

| カテゴリ | 項目                              |
| ---- | ------------------------------- |
| 生活負担 | 給食・弁当 / 持ち物 / おむつ対応 / 布団対応      |
| 時間負担 | 延長保育の時間 / 延長保育利用者 / 平日行事 / 保護者会 |

**機能**

| 機能         | 一般ユーザー | プレミアムユーザー  |
| ---------- | ------ | ---------- |
| 2園横並び比較    | ✅      | ✅          |
| 3園横並び比較    | ❌      | ✅          |
| 希望条件との一致表示 | ❌      | ✅（ハイライト表示） |

**一般ユーザーの比較画面**

事実ベースの情報を横並びで表示する。一致表示は行わない。

```txt
               ○○保育園    △△こども園
────────────────────────────────────────
【生活負担】
給食・弁当      毎日給食あり  週1弁当あり
持ち物          タオル・着替え ○○のみ
おむつ対応      園で廃棄      持ち帰り
布団対応        持参          レンタル

【時間負担】
延長保育の時間  18:00〜20:00  18:00〜19:00
延長保育利用者  3人程度       1人程度
平日行事        なし          年10回以上
保護者会        月に1回       年4回
```

**プレミアムユーザーの比較画面**

マイページ（プロフィール編集）で設定した希望条件に一致する項目を別色（背景色）でハイライト表示する。

```txt
あなたの希望条件との一致：

               ○○保育園      △△こども園    □□保育所
────────────────────────────────────────────────
【生活負担】
給食・弁当    [ 毎日給食あり ] 週1弁当あり    [ 毎日給食あり ]
持ち物          タオル・着替え  ○○のみ         タオル・着替え
おむつ対応    [ 園で廃棄 ]     持ち帰り        持ち帰り
布団対応        持参            レンタル        レンタル

【時間負担】
延長保育の時間  18:00〜20:00   19:00〜20:00    19:00〜20:00
延長保育利用者  3人程度         1人程度         1人程度
平日行事      [ なし ]         年10回以上      年8回
保護者会        月に1回         年4回           年3回

────────────────────────────────────────────────
[  ] = 希望条件に一致する項目
```

---

### 4.7 ログイン画面

**URL**

`/login`

**目的**

登録済みユーザーがメールアドレスとパスワードを入力してログインできるようにする。

**利用 API（Supabase Auth）**

| メソッド                          | 内容             |
| ----------------------------- | -------------- |
| Supabase `signInWithPassword` | メール・パスワードでログイン |

**表示項目**

| 項目          | 内容                |
| ----------- | ----------------- |
| メールアドレス入力欄  | ログイン用メールアドレス      |
| パスワード入力欄    | ログイン用パスワード        |
| ログインボタン     | 入力内容を送信する         |
| 会員登録画面へのリンク | `/register` へ遷移する |
| エラーメッセージ    | 認証失敗時に表示する        |

**バリデーション**

| 項目      | 条件       |
| ------- | -------- |
| メールアドレス | 必須・メール形式 |
| パスワード   | 必須・8文字以上 |

**操作**

* ログイン成功時は `/` へ遷移する
* ログイン失敗時はエラーメッセージを表示する
* セッションは Supabase Auth のデフォルト動作で自動ログインを維持する

**ログイン後のユーザー作成方針**

ログイン成功後、フロントエンドは `GET /users/me` を実行する。
バックエンド側では、Supabase Auth上のユーザーは存在するがアプリ側 `users` レコードが存在しない場合、一般ユーザーとして自動作成する。

**画面イメージ**

```txt
[ENKATSU]                               ☰

ログイン

メールアドレス
[ input ]

パスワード
[ input ]

[ ログイン ]

アカウントをお持ちでない方はこちら → /register
```

---

### 4.8 会員登録画面

**URL**

`/register`

**目的**

新しいユーザーがアカウントを作成できるようにする。登録時は一般ユーザーとして作成する。

ENKATSUにおける会員登録は、無料のユーザー登録を指す。
プレミアム登録は、会員登録後にStripe Checkoutで決済を完了した場合に行われる。

**利用 API（Supabase Auth）**

| メソッド              | 内容             |
| ----------------- | -------------- |
| Supabase `signUp` | メール・パスワードで新規登録 |

**表示項目**

| 項目          | 内容             |
| ----------- | -------------- |
| メールアドレス入力欄  | 登録用メールアドレス     |
| パスワード入力欄    | 登録用パスワード       |
| 登録ボタン       | 入力内容を送信する      |
| ログイン画面へのリンク | `/login` へ遷移する |
| エラーメッセージ    | 登録失敗時に表示する     |

**バリデーション**

| 項目      | 条件            |
| ------- | ------------- |
| メールアドレス | 必須・メール形式・重複不可 |
| パスワード   | 必須・8文字以上      |

**操作**

* 登録成功時は `/login` へ遷移する
* 登録失敗時はエラーメッセージを表示する
* 登録後、ログインしたタイミングで `GET /users/me` を実行し、必要に応じてアプリ側 `users` レコードを自動作成する

---

### 4.9 マイページ（一般ユーザー）

**URL**

`/mypage`

**目的**

ログイン済みユーザーが自分のアカウント情報・現在のプランでできることを確認し、プレミアムへのアップグレードを検討できるようにする。

**利用 API**

| メソッド | エンドポイント             | 内容                         |
| ---- | ------------------- | -------------------------- |
| GET  | `/users/me`         | ログインユーザー情報取得               |
| POST | `/payment/checkout` | Stripe Checkout Session 作成 |

**表示項目**

| 項目           | 内容                                                                               |
| ------------ | -------------------------------------------------------------------------------- |
| お名前          | 登録名を表示する                                                                         |
| 会員状態バッジ      | 「一般会員」と表示する                                                                      |
| プロフィール編集ボタン  | `/mypage/edit` へ遷移する                                                             |
| お気に入り一覧ボタン   | `/mypage/favorites` へ遷移する                                                        |
| 現在のプランでできること | 園一覧・園詳細の閲覧／お気に入り登録（5件まで）／2つの園を同時比較                                               |
| プレミアム誘導セクション | プレミアムの機能（3園比較・一致表示・サポート情報閲覧・お気に入り無制限）を紹介し、ボタン押下で直接 Stripe Checkout へ遷移する（確認画面なし） |
| ログアウトボタン     | ログアウト処理を行う                                                                       |

**画面イメージ**

```txt
[ENKATSU]                               ☰

マイページ                    ログアウト

[アバター] ○○さん
           一般会員

プロフィール編集 >
お気に入り一覧   >

現在のプランでできること
・園一覧・園詳細の閲覧
・お気に入り登録（5件まで）
・2つの園を同時比較

─────────────────────────────
✨ プレミアムでできること
・3つの園を同時比較
・希望条件との一致がひと目でわかる
・サポート情報が全て見られる
・お気に入りを無制限に登録できる

[ プレミアム会員に登録する ] → Stripe → /payment/complete
```

---

### 4.10 マイページ（プレミアムユーザー）

**URL**

`/mypage`

**目的**

プレミアムユーザーが自分のアカウント情報・プレミアム状態を確認し、必要であれば一般プランへ変更できるようにする。

**利用 API**

| メソッド | エンドポイント                    | 内容                            |
| ---- | -------------------------- | ----------------------------- |
| GET  | `/users/me`                | ログインユーザー情報取得                  |
| POST | `/payment/customer-portal` | Stripe Customer Portal URL 作成 |

**表示項目**

| 項目            | 内容                                 |
| ------------- | ---------------------------------- |
| お名前           | 登録名を表示する                           |
| 会員状態バッジ       | 「プレミアムユーザー」と表示する                   |
| プロフィール編集ボタン   | `/mypage/edit` へ遷移する               |
| お気に入り一覧ボタン    | `/mypage/favorites` へ遷移する          |
| 現在のプランでできること  | 3つの園を同時比較／お気に入りを無制限に保存／サポート情報を閲覧可能 |
| 一般プランに変更セクション | 変更後にロックされる機能の説明＋「変更する」ボタン          |

**一般プランに変更セクションの表示内容**

```txt
一般プランに変更する
下記の機能に変更になります
・2つの園を同時比較
・5件までお気に入り機能の追加
・マイページから希望条件の登録
```

**操作**

* 「変更する」ボタンを押すと、確認画面は表示せず直接 Stripe カスタマーポータルへ遷移する
* Stripe 側で解約操作を行う
* 解約完了後 `/payment/cancel-complete` へ遷移する
* `/payment/cancel-complete` はログイン済みユーザーであれば、一般・プレミアムどちらも表示可能とする

**画面イメージ**

```txt
[ENKATSU]                               ☰

マイページ                    ログアウト

[アバター] ○○ ○○さん
           プレミアムユーザー

プロフィール編集 >
お気に入り一覧   >

現在のプランでできること
・3つの園を同時比較
・お気に入りを無制限に保存
・サポート情報を閲覧可能

一般プランに変更する
下記の機能に変更になります
・2つの園を同時比較
・5件までお気に入り機能の追加
・マイページから希望条件の登録

[ 変更する ] → Stripe カスタマーポータル → /payment/cancel-complete
```

---

### 4.11 プロフィール編集画面

**URL**

`/mypage/edit`

**目的**

ログイン済みユーザーが自分のプロフィール情報・希望条件を編集できるようにする。希望条件はホーム画面のおすすめ表示・プレミアムの比較画面での一致表示に使用される。

**利用 API**

| メソッド | エンドポイント     | 内容       |
| ---- | ----------- | -------- |
| GET  | `/users/me` | ユーザー情報取得 |
| PUT  | `/users/me` | ユーザー情報更新 |

**表示項目**

| 項目       | 内容                                                |
| -------- | ------------------------------------------------- |
| お名前      | 表示名を編集する（必須）                                      |
| お住まいのエリア | 郵便番号入力で住所を自動入力する（必須）                              |
| 希望条件     | 生活負担・時間負担・補助情報に関する希望をチェックボックスで設定する                |
| 保存ボタン    | 編集内容を保存する。保存完了時は Toast 通知「保存しました」を表示する（別画面への遷移なし） |
| 戻るボタン    | `/mypage` へ戻る                                     |

**郵便番号自動入力**

zipcloud API（無料）を使用し、7桁入力時に都道府県・市区町村を自動入力する。自動入力後にユーザーが手修正できる。

DB上は `postal_code`、`address` を NULL 許可とするが、プロフィール編集画面ではおすすめ表示に利用するため、郵便番号・住所を必須入力とする。

**希望条件とおすすめ・比較への反映**

```txt
希望条件を設定する
↓
ホーム画面：エリア絞り込み後、希望条件との一致度順に表示
比較画面（プレミアム）：希望条件に一致する項目をハイライト表示

希望条件を設定しない
↓
ホーム画面：エリア（住所の市区町村）が近い順に表示
比較画面：一致表示なし（一般ユーザーと同様の表示）
```

**バリデーション**

| 項目             | 条件         |
| -------------- | ---------- |
| お名前            | 必須         |
| お住まいのエリア（郵便番号） | 必須・半角数字・7桁 |
| 住所             | 必須         |

**画面イメージ**

```txt
[ENKATSU]                               ☰

< 戻る

プロフィール編集

お名前 ※必須
[ 例）園活 太郎 ]

お住まいのエリア ※必須
[ 郵便番号 ]

住所 ※必須
[ 東京都○○区 ]

希望条件

生活負担
☐ 毎日給食
☐ おむつ廃棄
☐ 布団持参なし

時間負担
☐ 延長保育の利用時間〜19時まで
☐ 平日行事なし
☐ 保護者会なし

補助情報
☐ 園内習い事あり
☐ アレルギー対応あり

[ 保存 ]
```

---

### 4.12 プラン・料金画面

**URL**

`/plans`

**目的**

全ユーザーが「会員でない場合」「会員（一般ユーザー）」「プレミアムユーザー」それぞれで利用できる機能を比較し、登録・決済へ進めるようにする。

**利用 API**

| メソッド | エンドポイント             | 内容                         |
| ---- | ------------------- | -------------------------- |
| POST | `/payment/checkout` | Stripe Checkout Session 作成 |

**表示項目**

| セクション         | 内容                                                 |
| ------------- | -------------------------------------------------- |
| 会員でない方でも使える機能 | 園一覧閲覧・園詳細閲覧（基本情報のみ）・条件検索                           |
| 会員ユーザー（一般）    | 上記＋お気に入り登録（5件まで）・2園比較・新規会員登録ボタン                    |
| プレミアムユーザー     | 月額500円。上記＋お気に入り無制限・3園比較・希望条件一致表示・サポート情報閲覧・決済に進むボタン |

**ボタンの表示制御**

| ユーザー区分    | 「新規会員登録はこちら」        | 「プレミアム会員に登録する」          |
| --------- | ------------------- | ----------------------- |
| 未登録ユーザー   | 有効（`/register` へ遷移） | 有効（`/register` へ遷移）     |
| 一般ユーザー    | グレーアウト（disabled）    | 有効（Stripe Checkout へ遷移） |
| プレミアムユーザー | グレーアウト（disabled）    | グレーアウト（disabled）        |

**画面イメージ**

```txt
[ENKATSU]                               ☰

プラン・料金

─────────────────────────────
会員でない方でも下記機能が使えます
・園一覧閲覧
・園詳細閲覧（基本情報）
・条件検索

─────────────────────────────
会員ユーザー
・お気に入り登録（5件まで）
・2園比較
                 [ 新規会員登録はこちら ]

─────────────────────────────
プレミアムユーザー
月額500円
・お気に入り無制限
・3園比較
・希望条件との一致表示
・サポート情報閲覧
                       [ 決済に進む ]
```

---

### 4.13 プレミアム登録完了画面

**URL**

`/payment/complete`

**目的**

Stripe Checkout 後にプレミアム登録の完了を伝え、ホームへ誘導する。

**表示項目**

| 項目      | 内容                |
| ------- | ----------------- |
| 完了メッセージ | 「プレミアム登録が完了しました！」 |
| ホームへの導線 | `/` へ遷移するボタン      |

**画面イメージ**

```txt
[ENKATSU]                               ☰

プレミアム登録が完了しました！

3園比較・お気に入り無制限・サポート情報閲覧を
ご利用いただけます。

[ ホームへ戻る ]
```

---

### 4.14 プレミアムキャンセル完了画面

**URL**

`/payment/cancel-complete`

**目的**

プレミアムプランの解約完了を伝え、ホームへ誘導する。

Stripe Customer Portal からの戻り先であるため、ログイン済みユーザーであれば一般ユーザー・プレミアムユーザーのどちらも表示可能とする。

**表示項目**

| 項目        | 内容                     |
| --------- | ---------------------- |
| 完了メッセージ   | 「一般会員への変更が完了しました」      |
| 利用可能期間の案内 | 「○月○日までプレミアム機能が使用できます」 |
| ホームへの導線   | `/` へ遷移するボタン           |

**プレミアム解約の流れ**

```txt
マイページの「変更する」ボタン
↓
確認画面なし・直接 Stripe カスタマーポータルへ遷移
↓
Stripe 側で解約操作
↓
/payment/cancel-complete へ遷移
```

**Webhook 実装について**

Stripe からの通知（Webhook）をバックエンドで受け取り、プレミアム状態を更新する処理が必須。

プレミアム判定は `subscriptions.status = active` を正とする。

| イベント    | 処理                 |
| ------- | ------------------ |
| 毎月の決済成功 | プレミアム継続            |
| 決済失敗    | プレミアム停止または要確認状態に更新 |
| 解約      | プレミアム停止            |

**画面イメージ**

```txt
[ENKATSU]                               ☰

一般会員への変更が完了しました

○月○日までプレミアム機能が使用できます

[ ホームへ戻る ]
```

---

### 4.15 404エラー画面

**目的**

存在しない URL へのアクセス時にわかりやすいメッセージを表示し、ホームへ誘導する。

**画面イメージ**

```txt
[ENKATSU]                               ☰

お探しのページが見つかりませんでした。

[ ホームへ戻る ]
```

---

### 4.16 ローディング画面

**目的**

ページ遷移中・データ取得中にローディングを表示し、ユーザーに処理中であることを伝える。

**表示項目**

| 項目   | 内容                 |
| ---- | ------------------ |
| ロゴ   | ENKATSU のロゴを表示する   |
| スピナー | ローディングアニメーションを表示する |

---

## 5. 画面遷移

### 5.1 未登録ユーザー

```txt
/
  ├─ 検索（保育園名・住所）       → /schools/search
  ├─ 条件で検索                   → /schools/search（条件をクエリに付与）
  ├─ SchoolCard クリック          → /schools/[id]
  └─ ハンバーガーメニュー
      ├─ 新規会員登録             → /register
      ├─ ログイン                 → /login
      └─ プラン・料金確認         → /plans

/schools/search
  └─ SchoolCard クリック          → /schools/[id]

/schools/[id]
  ├─ FavoriteButton（♡）         → ログイン誘導モーダル → /login
  ├─ サポート情報タップ           → 会員登録誘導モーダル → /register
  └─ 戻る                        → /schools/search

/login
  └─ ログイン成功                 → /

/register
  └─ 登録成功                     → /login

/plans
  ├─ 新規会員登録はこちら         → /register
  └─ 決済に進む（未登録）         → /register
```

### 5.2 一般ユーザー

```txt
/
  ├─ 検索（保育園名・住所）       → /schools/search
  ├─ 条件で検索                   → /schools/search（条件をクエリに付与）
  └─ SchoolCard クリック          → /schools/[id]

/schools/[id]
  ├─ FavoriteButton（♡）         → お気に入り登録・解除（Toast 通知）
  └─ サポート情報タップ           → プレミアム誘導モーダル → /plans

/mypage/favorites
  ├─ 解除ボタン                   → /mypage/favorites（同画面更新）
  └─ 比較する（2園選択）          → /compare

/compare
  └─ 戻る                        → /mypage/favorites

/mypage
  ├─ プロフィール編集             → /mypage/edit
  ├─ お気に入り一覧               → /mypage/favorites
  ├─ プレミアム会員に登録する     → Stripe Checkout → /payment/complete
  └─ ログアウト                   → /

/mypage/edit
  └─ 保存                        → Toast通知「保存しました」（同画面）

/payment/complete
  └─ ホームへ戻る                 → /

/payment/cancel-complete
  └─ ホームへ戻る                 → /
```

### 5.3 プレミアムユーザー

```txt
/
  ├─ 検索（保育園名・住所）       → /schools/search
  ├─ 条件で検索                   → /schools/search（条件をクエリに付与）
  └─ SchoolCard クリック          → /schools/[id]

/schools/[id]
  └─ FavoriteButton（♡）         → お気に入り登録・解除（Toast 通知）

/mypage/favorites
  ├─ 解除ボタン                   → /mypage/favorites（同画面更新）
  └─ 比較する（3園選択）          → /compare

/compare
  └─ 戻る                        → /mypage/favorites

/mypage
  ├─ プロフィール編集             → /mypage/edit
  ├─ お気に入り一覧               → /mypage/favorites
  ├─ 変更する（一般プランへ）     → Stripe カスタマーポータル → /payment/cancel-complete
  └─ ログアウト                   → /

/mypage/edit
  └─ 保存                        → Toast通知「保存しました」（同画面）

/payment/cancel-complete
  └─ ホームへ戻る                 → /
```

---

## 6. 認証・認可による画面制御

| 画面                         | 未登録              | 一般               | プレミアム           |
| -------------------------- | ---------------- | ---------------- | --------------- |
| `/`                        | ✅                | ✅                | ✅               |
| `/schools/search`          | ✅                | ✅                | ✅               |
| `/schools/[id]`            | ✅（サポート情報はオーバーレイ） | ✅（サポート情報はオーバーレイ） | ✅（全情報閲覧可）       |
| `/login`                   | ✅                | リダイレクト（`/`）      | リダイレクト（`/`）     |
| `/register`                | ✅                | リダイレクト（`/`）      | リダイレクト（`/`）     |
| `/plans`                   | ✅                | ✅（新規会員登録はグレーアウト） | ✅（両ボタングレーアウト）   |
| `/mypage/favorites`        | ❌ → `/login`     | ✅（5件・2園比較）       | ✅（無制限・3園比較）     |
| `/compare`                 | ❌ → `/login`     | ✅（一致表示なし）        | ✅（一致表示あり）       |
| `/mypage`                  | ❌ → `/login`     | ✅（プレミアム誘導表示）     | ✅（プレミアム状態・解約導線） |
| `/mypage/edit`             | ❌ → `/login`     | ✅                | ✅（希望条件設定あり）     |
| `/payment/complete`        | ❌ → `/login`     | ✅                | ✅               |
| `/payment/cancel-complete` | ❌ → `/login`     | ✅                | ✅               |

**制御方針**

* 未ログインユーザーが認証必須画面へアクセスした場合は `/login` へ遷移する
* 一般ユーザーがプレミアム限定機能にアクセスした場合は LockOverlay を表示し `/plans` へ誘導する
* 園詳細画面のサポート情報は、未登録ユーザー・一般ユーザーには項目名のみを表示し、値の部分にスモーク（ぼかし）をかけた上に「プレミアムユーザーに登録すれば閲覧可能」というテキスト付きのオーバーレイを重ねて表示する
* サポート情報のロック表示をタップした場合、未登録ユーザーは `/register`、一般ユーザーは `/plans` へ誘導する
* 画面側のルートガードに加え、バックエンド API 側でも必ず認可チェックを行う
* プレミアム判定は `subscriptions.status = active` を正とする
* お気に入り件数は一般ユーザー5件・プレミアム無制限とし、バックエンド側でも制限チェックを行う
* `/plans` のボタン表示制御（グレーアウト）はログイン状態・会員区分をもとにフロントエンドで判定する
* `/payment/cancel-complete` は Stripe Customer Portal からの戻り先であるため、ログイン済みユーザーであれば一般ユーザー・プレミアムユーザーのどちらも表示可能とする

---

## 7. MVP 対象外の画面・機能

| 分類  | 内容                                      |
| --- | --------------------------------------- |
| 画面  | 詳細比較レポート画面（`/report`）                   |
| 機能  | 口コミ投稿・口コミ承認                             |
| 機能  | 人気ランキング                                 |
| 機能  | AI レポート生成                               |
| 機能  | 地図機能                                    |
| 機能  | 通知機能                                    |
| 機能  | PDF 出力                                  |
| 機能  | 請求履歴表示・プラン変更（アップグレード以外）                 |
| 機能  | 高度なレコメンド機能                              |
| 画面  | 管理画面 CRUD                               |
| 画面  | `/compare/select`（比較リスト選択画面・お気に入り一覧に統合） |
| データ | 実在園データの大量登録（MVP は seed データで対応）          |

---

## 8. 変更履歴

| バージョン | 日付         | 変更内容                                                                                                                                                                                                                                                                                                            |
| ----- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v0.1  | 2026/06/10 | 初版作成                                                                                                                                                                                                                                                                                                            |
| v0.2  | 2026/06/11 | Figmaデザインをもとに全面更新。URL を `/schools` に変更、ボトムナビ廃止・ハンバーガーメニューに変更、チャート廃止・希望条件一致度表示に変更、各種画面の追加・統合                                                                                                                                                                                                                     |
| v0.3  | 2026/06/12 | お気に入り機能を一般ユーザーに開放（5件まで）、サポート情報をプレミアム限定に変更、「比較リストに追加」を「お気に入り登録」に統一、`/compare/select` 廃止・お気に入り一覧から比較対象を選択する形式に変更、プレミアム比較画面に希望条件ハイライト表示を追加、`/plans` をプラン比較形式に変更、ハンバーガーメニューに「プラン・料金確認」追加・「比較」削除、一般ユーザーの比較画面・マイページ・決済完了画面の内容を調整                                                                                      |
| v0.4  | 2026/06/13 | フッター削除、詳細比較レポート画面（`/report`）を削除、検索条件を保育園名・住所のみに簡略化、SchoolCardに電話番号追加・カード全体をリンク化、園詳細画面の項目名を「毎日の準備」「仕事との両立」に変更（比較画面は「生活負担」「時間負担」を維持）、時間負担の表示項目を見直し、「詳細説明」→「園の特徴」に変更、ホーム画面のおすすめ表示ロジック追加、ログイン画面のプラン選択導線を削除、プレミアムマイページの解約フローを簡略化、プラン・料金画面にボタングレーアウト制御を追加、ハンバーガーメニューを「お気に入り/比較」に変更                                    |
| v0.5  | 2026/06/14 | 園詳細画面のサポート情報制限表示をテキスト付きオーバーレイに変更、ホーム画面・検索結果画面に開閉式の「条件で検索」を追加、SchoolCardにタグ表示を追加、一般ユーザーのマイページに「現在のプランでできること」セクションを追加・プレミアム誘導ボタンを直接Stripe遷移に変更、マイページのレイアウトをアバター＋リンクリスト形式に統一、プレミアムマイページに「お気に入り一覧」ボタンを追加                                                                                                            |
| v0.6  | 2026/06/15 | 4.10プレミアムマイページの一般プラン変更セクションの表示内容を更新（変更後の機能を明示）、4.4園詳細画面のタグを「検索条件に該当するもの」のみ表示する形に変更・セクション名の使い分けを明記、4.11プロフィール編集画面の希望条件項目を「生活負担・時間負担・補助情報」の3カテゴリに整理・保存完了をToast通知に変更・郵便番号を「お住いのエリア（必須）」に変更、4.12プラン・料金画面の未登録ユーザーの「プレミアム会員に登録する」遷移先を `/register` に変更、4.2ホーム画面（一般・プレミアム）に「条件で検索」の操作を追加、6.認証・認可のサポート情報表示説明をオーバーレイ表示に更新 |
| v0.7  | 2026/06/22 | 要件定義書・API設計書・DB設計書との整合性確認結果を反映。冒頭の2軸説明を「生活負担・時間負担」に統一し、園詳細画面では「毎日の準備」「仕事との両立」を使用する方針を明記。比較画面の利用APIを `GET /schools/compare?ids=...` に修正。サポート情報ロックの誘導先を未登録ユーザーは `/register`、一般ユーザーは `/plans` に分岐。プレミアム判定表記を `subscriptions.status = active` に統一                                                                   |
| v0.8  | 2026/06/23 | `/payment/cancel-complete` は Stripe Customer Portal からの戻り先であるため、ログイン済みユーザーであれば一般・プレミアムどちらも表示可能に変更。RedisキャッシュをMVP必須として、ホーム画面・検索結果画面の取得方針に明記。会員登録後の `users` レコード自動作成方針をログイン画面・会員登録画面に追記。プロフィール編集画面の「お住まいのエリア」表記と住所必須を明記。                                                                                          |

---

## 9. 備考

本画面設計書は、チーム開発時の認識合わせを目的とした初期ドキュメントである。

実装中に画面構成や仕様が変更になった場合は、Issue または PR コメントに理由を残し、必要に応じて本ドキュメントを更新する。

画面デザインは MVP では細かい装飾よりも、操作しやすさ・認証認可の分かりやすさ・ユーザー区分ごとの機能制限の明確さを優先する。

**セクション名の使い分けについて**

園詳細画面では「毎日の準備」「仕事との両立」という表現を使用する。これは園側の情報がネガティブなイメージにならないようにするためである。比較画面では保護者目線で負担度合いを比較する目的から「生活負担」「時間負担」を使用する。
