# ENKATSU E2Eテストシナリオ

最終更新日：2026/07/08
対象：テスト設計書 7. Frontend手動確認観点（FE-001〜024）＋ 管理者サイト
使用ツール：Playwright

## 1. 概要

本ドキュメントは、ENKATSUのFrontend主要導線をPlaywrightでE2Eテスト化した内容をまとめたものです。

未登録ユーザー・一般ユーザー・プレミアムユーザーの3区分（テスト設計書FE-001〜024対応）に加え、保育園管理者サイト（`/admin`配下）のE2Eテストも実装しています。

## 2. テストファイル構成

```
frontend/
├── playwright.config.ts
└── e2e/
    ├── auth.setup.ts          ログイン状態を作成し、認証情報を保存する
    ├── guest-user.spec.ts     未登録ユーザーの主要導線（FE-001〜006）
    ├── general-user.spec.ts   一般ユーザーの主要導線（FE-009〜017）
    ├── premium-user.spec.ts   プレミアムユーザーの主要導線（FE-019〜024）
    ├── admin-login.spec.ts    管理者ログイン画面（未認証で毎回操作）
    └── admin.spec.ts          管理者ホーム・編集・ハンバーガーメニュー（認証済み）
```

## 3. 実行環境・前提条件

### 3-1. 必要な準備

- Docker Composeでfrontend（localhost:3000）・backendが起動していること
- `frontend/.env.test.local`（gitignore対象）にテスト用アカウントの認証情報が設定されていること

```
E2E_GENERAL_USER_EMAIL=（一般ユーザーのメールアドレス）
E2E_GENERAL_USER_PASSWORD=（一般ユーザーのパスワード）
E2E_PREMIUM_USER_EMAIL=（プレミアムユーザーのメールアドレス）
E2E_PREMIUM_USER_PASSWORD=（プレミアムユーザーのパスワード）
E2E_ADMIN_EMAIL=（保育園管理者アカウントのメールアドレス）
E2E_ADMIN_PASSWORD=（保育園管理者アカウントのパスワード）
```

### 3-2. 初回セットアップ

```powershell
cd frontend
npm install
npx playwright install
```

### 3-3. 実行コマンド

```powershell
npm run test:e2e          # 全テスト実行
npm run test:e2e:ui       # UIモードでのデバッグ実行
npm run test:e2e:report   # 直近のHTMLレポート表示
```

特定のテストのみ実行する場合：

```powershell
npm run test:e2e -- -g "FE-013"
npm run test:e2e -- -g "管理者"
```

## 4. 認証の仕組み

Playwrightの`storageState`機能を利用し、テストごとにUIからログインし直す代わりに、事前にログイン済みの状態（Cookie・トークン）をファイルに保存して使い回しています。

| プロジェクト名               | 内容                                                         | 認証状態                             |
| ---------------------------- | ------------------------------------------------------------ | ------------------------------------ |
| `setup`                      | `auth.setup.ts`を実行し、3アカウント分の認証状態を保存       | -                                    |
| `chromium`                   | 未登録ユーザー向けテスト（デスクトップ）                     | なし                                 |
| `mobile-chrome`              | 未登録ユーザー向けテスト（モバイル）                         | なし                                 |
| `general-user-authenticated` | 一般ユーザー向けテスト                                       | `playwright/.auth/general-user.json` |
| `premium-user-authenticated` | プレミアムユーザー向けテスト                                 | `playwright/.auth/premium-user.json` |
| `admin-authenticated`        | 保育園管理者向けテスト（ホーム・編集・ハンバーガーメニュー） | `playwright/.auth/admin-user.json`   |

`setup`プロジェクトが最初に実行され、他のプロジェクトはこれに依存（`dependencies: ['setup']`）する形になっています。

管理者のログイン画面自体（`admin-login.spec.ts`）は未認証状態を毎回検証する必要があるため、`chromium` / `mobile-chrome`プロジェクトで実行し、`admin-authenticated`には含めていません。

## 5. テストシナリオ一覧

### 5-1. 未登録ユーザー（`guest-user.spec.ts`）

| ID     | シナリオ                             | 確認内容                                                                   |
| ------ | ------------------------------------ | -------------------------------------------------------------------------- |
| FE-001 | ホーム画面が表示される               | ホーム画面のキャッチコピーが表示される                                     |
| FE-002 | 園検索ができる                       | 検索ボックスにキーワードを入力し、検索結果画面へ遷移する                   |
| FE-003 | 園詳細画面へ遷移できる               | 検索結果から園カードをクリックし、詳細画面へ遷移する                       |
| FE-004 | サポート情報がロック表示される       | 園詳細画面でロックボタンを押すと会員登録モーダルが表示される               |
| FE-005 | お気に入り押下時にログイン誘導される | お気に入りボタン押下でログイン誘導モーダルが開き、ログイン画面へ遷移できる |
| FE-006 | プラン・料金画面へ遷移できる         | ハンバーガーメニューから`/plans`へ遷移する                                 |

実行プロジェクト：`chromium` / `mobile-chrome`（認証不要）

備考：

- FE-001は、ホーム画面のキャッチコピーに全角の引用符（“ ”）が使われているため、テスト側は引用符に依存しない正規表現でマッチさせています。今後コピーが変わった場合はこの正規表現の見直しが必要です。

### 5-2. 一般ユーザー（`general-user.spec.ts`）

| ID     | シナリオ                    | 確認内容                                                                                           |
| ------ | --------------------------- | -------------------------------------------------------------------------------------------------- |
| FE-010 | プロフィールを保存できる    | プロフィール編集画面で必須項目（お名前・郵便番号・住所）を入力して保存し、成功トーストが表示される |
| FE-011 | お気に入り登録できる        | 検索結果からお気に入りボタンを押し、登録済み状態に変わる                                           |
| FE-012 | お気に入り解除できる        | お気に入り一覧から解除し、件数が減る                                                               |
| FE-013 | お気に入り上限が表示される  | 「x / 3件登録中」の上限表示がされる                                                                |
| FE-014 | 2園比較できる               | お気に入りから2園選択し、比較画面へ遷移する                                                        |
| FE-015 | 3園比較は利用できない       | 3園選択時に上限超過の警告トーストが表示される                                                      |
| FE-016 | サポート情報がロックされる  | 一般ユーザーでもロック表示され、プレミアム誘導が機能する                                           |
| FE-017 | Stripe Checkoutへ遷移できる | プラン画面でCheckoutボタンが有効になっている                                                       |
| FE-009 | ログアウトできる            | ログアウト後、未ログイン状態のメニューに戻る                                                       |

実行プロジェクト：`general-user-authenticated`

備考：

- ファイル内は`describe.configure({ mode: 'serial' })`で直列実行しています。これは`supabase.auth.signOut()`がデフォルトで全セッションを無効化する仕様のため、ログアウトのテスト（FE-009）が他のテストのセッションを巻き込まないよう、最後に実行する構成にしているためです。
- お気に入り関連テスト（FE-012〜014）は、検索結果（seedデータ）の件数に左右されないよう、各テスト実行前に既存のお気に入りを全解除してから必要な件数だけ登録するヘルパー関数を使用しています。
- FE-010は、テストアカウントの既存プロフィールデータ（名前・郵便番号・住所）が空だと、クライアント側バリデーションで弾かれて保存処理（Toast表示）まで到達しない不具合があったため、テスト内で毎回有効な値を入力してから保存する形に修正済みです。
- FE-013は、一般ユーザーのお気に入り上限が5件→3件に仕様変更されたことに伴い、期待値を更新済みです。上限値は今後も変わる可能性があるため、仕様変更時はこのテストの期待値も併せて見直してください。

### 5-3. プレミアムユーザー（`premium-user.spec.ts`）

| ID     | シナリオ                         | 確認内容                                                     |
| ------ | -------------------------------- | ------------------------------------------------------------ |
| FE-019 | マイページでプレミアム表示になる | 「プレミアムユーザー」の表示がされる                         |
| FE-020 | お気に入りを6件以上登録できる    | 上限なく6件登録でき、件数表示に上限がない                    |
| FE-021 | 3園比較できる                    | お気に入りから3園選択し、比較画面へ遷移する                  |
| FE-022 | 希望条件との一致表示が出る       | 比較画面で「あなたの希望条件との一致」セクションが表示される |
| FE-023 | 園詳細でサポート情報が表示される | ロックされず、サポート情報の内容が閲覧できる                 |
| FE-024 | Customer Portalへ遷移できる      | マイページの「変更する」ボタンでポータルへの遷移が開始する   |

実行プロジェクト：`premium-user-authenticated`

備考：

- FE-021・FE-022は、各テスト内で必要な件数のお気に入りを都度登録する自己完結型の実装にしています。これは他のテストの実行結果やskipの影響を受けないようにするためです。
- FE-022は、テスト用アカウントに希望条件（`user_preferences`）が事前に設定されていることが前提です。
- FE-017・FE-024は、Stripeの実際の決済・ポータル画面までは検証せず、ボタン押下・画面遷移の開始までを確認しています。

### 5-4. 保育園管理者（`admin-login.spec.ts` / `admin.spec.ts`）

| ID  | ファイル            | シナリオ                                                 | 確認内容                                                                 |
| --- | ------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------ |
| -   | admin-login.spec.ts | アカウントID・パスワード未入力でエラー表示               | 入力チェックのエラーメッセージが表示される                               |
| -   | admin-login.spec.ts | パスワードが6文字未満でエラー表示                        | 文字数チェックのエラーメッセージが表示される                             |
| -   | admin-login.spec.ts | 誤った認証情報でエラー表示                               | 「アカウントIDまたはパスワードが正しくありません」が表示される           |
| -   | admin-login.spec.ts | 「ENKATSUホーム画面に戻る」でトップページへ遷移          | `/`へ遷移する                                                            |
| -   | admin-login.spec.ts | 正しい認証情報でログインできる                           | 管理者ホーム（`/admin`）へ遷移する                                       |
| -   | admin.spec.ts       | 管理者ホームで園名・担当者名が実データで表示される       | プレースホルダー（●●保育園／●●●●）ではなく、取得した実データが表示される |
| -   | admin.spec.ts       | タブ切り替え（お問い合わせ一覧⇔見学予約一覧）            | タブごとに対応するリストが表示される                                     |
| -   | admin.spec.ts       | 「園情報編集」ボタンで編集画面へ遷移                     | `/admin/edit`へ遷移する                                                  |
| -   | admin.spec.ts       | 編集画面でフォーム入力・保存ができる                     | 入力後の保存操作で、成功／失敗いずれかのToastが表示される                |
| -   | admin.spec.ts       | 保育園の種別ラジオボタンを切り替えられる                 | 選択状態が排他的に切り替わる                                             |
| -   | admin.spec.ts       | ハンバーガーメニューにホーム・編集・ログアウト導線がある | 各リンク・ボタンが表示され、遷移できる                                   |
| -   | admin.spec.ts       | メニュー内に園名・担当者名が2行で表示される              | 園名・担当者名がそれぞれ別行で表示される                                 |
| -   | admin.spec.ts       | ログアウトでログイン画面へ遷移する                       | `/admin/login`へ遷移する                                                 |

実行プロジェクト：`admin-login.spec.ts` → `chromium` / `mobile-chrome`（認証不要）、`admin.spec.ts` → `admin-authenticated`

備考：

- 管理者アカウントは一般ユーザー・プレミアムユーザーと同じSupabase Authの仕組み（メール・パスワード）でログインしています。
- 編集画面の`label`と`input`が`htmlFor`/`id`で紐付いていないため、テストでは同一`div`内のsibling要素として`input`を取得しています。
- 「保存」テストは、バックエンドの`PATCH /school-admin/school`の実装状況によって成功／失敗いずれのToastが出ても許容する作りにしています。実装が安定したら成功ケースのみに絞ることを推奨します。

## 6. 既知の制約・注意点

### 6-1. バックエンドの並列負荷について

ローカル環境でテストを並列実行すると、バックエンド（Express + Redis）への同時アクセスが集中し、まれにAPIレスポンスが遅延・失敗することがあります。対策として`playwright.config.ts`で以下を設定しています。

- `retries: 1`（失敗時に1回まで自動リトライ）
- `workers`をCPUコア数のデフォルトより少なめに制限

### 6-2. 検索結果・お気に入り件数への依存

お気に入り関連のテストは、検索結果の件数（seedデータ）に依存すると不安定になりやすいため、テスト実行前に状態をリセットしてから必要な件数だけ登録する設計にしています。

### 6-3. UI要素の重なりによるクリック判定

`SchoolCard.tsx`では`FavoriteButton`が`absolute`配置で園詳細へのリンクと重なっているため、クリック位置を指定する（`position: { x: 10, y: 10 }`）ことで誤クリックを回避しています。

### 6-4. 表示文言の全角・半角の違い

`getByText`は完全一致でテキストノードを見るため、デザイン上使われている全角クォート（“ ”）と半角クォート（" "）のような見た目が似た文字の違いでテストが落ちることがあります（FE-001で実際に発生）。文言をテストに書き写す際は、コピー＆ペーストで実際のソースコードから取得するか、記号部分を正規表現でワイルドカード化するのが安全です。

### 6-5. 仕様変更されうる数値のハードコーディング

お気に入り上限件数（一般ユーザー：3件）のように、後から仕様変更されうる数値をテストにハードコーディングしている箇所があります（FE-013）。こうした数値がフロント・バックエンド・テストのどこか1箇所だけ更新されて他が追従できていない、という事態を防ぐため、可能であれば設定値を一元管理する仕組み（環境変数・定数ファイルの共有等）の導入を検討したいです。

## 7. 今後の拡張候補

- GitHub ActionsへのE2Eテスト組み込み（CI実行）
- Stripeテストモードでの決済・ポータル遷移の完全な検証
- 単体テスト（Vitest）との役割分担の明確化
- お気に入り上限などの仕様変更されうる数値の一元管理（フロント・バックエンド・テスト間の同期）
